# Environment Configuration for Admin Backend

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Database Connection
# Replace with your MongoDB connection string
# Local: mongodb://localhost:27017/rag-admin
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/rag-admin
MONGO_URI=mongodb://localhost:27017/rag-admin

# JWT Authentication Secret
# IMPORTANT: Generate a strong, random secret for production!
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Configuration
# Comma-separated list of allowed origins or use * for all (not recommended for production)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# FastAPI Bot Backend URL
# This is where your Python RAG chatbot is running
FASTAPI_BOT_URL=http://localhost:8000

# Optional: Additional Configuration
# LOG_LEVEL=debug
# MAX_REQUEST_SIZE=10mb
